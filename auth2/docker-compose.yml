services:
  postgres:
    image: postgres:latest
    container_name: postgres-auth2service
    environment:
      DB_POSTGRESQL_DATABASE_NAME: ${DB_POSTGRESQL_DATABASE_NAME}
      DB_POSTGRESQL_DATABASE_SCHEMA: ${DB_POSTGRESQL_DATABASE_SCHEMA}
      DB_POSTGRESQL_SCHEMA_NAME: ${DB_POSTGRESQL_SCHEMA_NAME}
      DB_POSTGRESQL_USER: ${DB_POSTGRESQL_USER}
      DB_POSTGRESQL_PASSWORD: ${DB_POSTGRESQL_PASSWORD}
      DB_POSTGRESQL_HOST: ${DB_POSTGRESQL_HOST}
      DB_POSTGRESQL_PORT: ${DB_POSTGRESQL_PORT}
      DB_POSTGRESQL_PORT_EXTERNAL: ${DB_POSTGRESQL_PORT_EXTERNAL}
      POSTGRES_DB: ${DB_POSTGRESQL_DATABASE_NAME}
      POSTGRES_USER: ${DB_POSTGRESQL_USER}
      POSTGRES_PASSWORD: ${DB_POSTGRESQL_PASSWORD}
    ports:
      - ${DB_POSTGRESQL_PORT_EXTERNAL}:${DB_POSTGRESQL_PORT}
    volumes:
      - postgres_data:/var/lib/postgresql/data
#      - ./init-db.sql:/docker-entrypoint-initdb.d/init-db.sql
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U ${DB_POSTGRESQL_PASSWORD}" ]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - online-store-auth2-network

  auth2service:
    image: auth2service
    build:
      context: .
      args:
        LOG_FOLDER: ${LOG_FOLDER}
        AUTH2_SERVICE_PORT: ${AUTH2_SERVICE_PORT}
        AUTH2_SERVICE_DEBUG_PORT: ${AUTH2_SERVICE_DEBUG_PORT}
    env_file:
      - .env
    environment:
      - JAVA_TOOL_OPTIONS=-agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:${AUTH2_SERVICE_DEBUG_PORT:5007}
      - AUTH2_SERVICE_PORT=${AUTH2_SERVICE_PORT}
      - AUTH2_SERVICE_DEBUG_PORT=${AUTH2_SERVICE_DEBUG_PORT}
      - LOG_FOLDER=${LOG_FOLDER}
      - LOGGING_LEVEL_ROOT=${LOGGING_LEVEL_ROOT}
      #- SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:${DB_POSTGRESQL_PORT}/${DB_POSTGRESQL_DATABASE_NAME}
      - SPRING_DATASOURCE_URL=r2dbc:postgresql://postgres:${DB_POSTGRESQL_PORT}/${DB_POSTGRESQL_DATABASE_NAME}
      - SPRING_DATASOURCE_DRIVER-CLASS-NAME=io.r2dbc.postgresql.PostgresqlConnectionFactory
      - SPRING_DATASOURCE_USERNAME=${DB_POSTGRESQL_USER}
      - SPRING_DATASOURCE_PASSWORD=${DB_POSTGRESQL_PASSWORD}
      - SPRING_LIQUIBASE_CHANGELOG=classpath:db/changelog/changelog-master.yaml
      - TOKEN_VALIDITY_TIMEOUT=${TOKEN_VALIDITY_TIMEOUT}
      - ANGULAR_CLIENT_URL=${ANGULAR_CLIENT_URL}
    depends_on:
      - postgres
    ports:
      - ${AUTH2_SERVICE_PORT}:${AUTH2_SERVICE_PORT}
      - ${AUTH2_SERVICE_DEBUG_PORT}:${AUTH2_SERVICE_DEBUG_PORT}
    container_name: auth2service
    networks:
      - online-store-auth2-network

networks:
  online-store-auth2-network:
    name: online-store-auth2-network
    driver: bridge
    attachable: true

volumes:
  postgres_data: